# Telegram Video Archive Bot — media for portfolio page

Add screenshots or GIFs here, then reference them from `telegram-video-archive-bot.html`.

Suggested files:

- `forward-demo.gif` — forwarding a video to the bot
- `category-picker.png` — inline category keyboard after save
- `batch-upload.gif` — multiple forwards + one category picker
- `videos-browse.png` — `/videos` category list or paginated preview

Replace the placeholder `<figure class="portfolio-figure media-placeholder">` blocks on the project page with real `<img>` tags (see `phy180-pendulum.html` for examples).
